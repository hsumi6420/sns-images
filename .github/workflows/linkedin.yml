name: LinkedIn Auto Post Test

on:
  workflow_dispatch:

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Get User ID
        run: |
          RESPONSE=$(curl -s -H "Authorization: Bearer ${{ secrets.LINKEDIN_ACCESS_TOKEN }}" https://api.linkedin.com/v2/userinfo)
          USER_ID=$(echo "$RESPONSE" | jq -r '.sub')
          
          if [ "$USER_ID" == "null" ] || [ -z "$USER_ID" ]; then
            RESPONSE=$(curl -s -H "Authorization: Bearer ${{ secrets.LINKEDIN_ACCESS_TOKEN }}" https://api.linkedin.com/v2/me)
            USER_ID=$(echo "$RESPONSE" | jq -r '.id')
          fi

          echo "User ID: $USER_ID"
          if [ "$USER_ID" == "null" ] || [ -z "$USER_ID" ]; then
            echo "Failed to get User ID. Response: $RESPONSE"
            exit 1
          fi

          echo "USER_ID=$USER_ID" >> $GITHUB_ENV

      - name: Post to LinkedIn
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST https://api.linkedin.com/v2/ugcPosts \
            -H "Authorization: Bearer ${{ secrets.LINKEDIN_ACCESS_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{
              "author": "urn:li:person:'"${{ env.USER_ID }}"'",
              "lifecycleState": "PUBLISHED",
              "specificContent": {
                "com.linkedin.ugc.ShareContent": {
                  "shareCommentary": {
                    "text": "GitHub Actionsからの自動投稿テストです！🚀"
                  },
                  "shareMediaCategory": "NONE"
                }
              },
              "visibility": {
                "com.linkedin.ugc.MemberNetworkVisibility": "PUBLIC"
              }
            }')
          
          HTTP_STATUS=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          echo "$BODY"
          
          if [ "$HTTP_STATUS" -ne 201 ]; then
            echo "Failed with HTTP status $HTTP_STATUS"
            exit 1
          fi
